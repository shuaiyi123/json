任务：1、解析.json文件，将所需的数据提取出来，然后放到指定的共享结构中，等待其他进程进行读取。
     2、其它进程可对共享的结构进行读写，当对共享的结构进行写时，需要将修改的结构的参数同步到.json文件中

手段：使用有名信号灯进行进程间的同步，函数如下：POSIX semaphore: sem_open, sem_close, sem_post, sem_wait

设计思路：创建一个共享结构，用来存放.json提取的数据，当本进程向结构写时，不允许其它其它进程读操作，因此需要加信号量解决互斥的问题。

基本框架：三个主源文件，devcfg_read.c负责提取.json文件，并将提取到的数据放到固定的结构中；devcfg_write.c负责将其它进程修改过的
        结构的参数写到.json文件中；gshm.c负责开辟一个共享文件，并将共享文件强制转换为固定结构体，以供其它进程进行读写。

工作进度：
    1、完成ver.json文件的解析(版本信息version)
    2、完成mfr.json文件的解析(生产商信息manufacture)
    3、完成yc.json文件的解析(采样模块信息)
    4、完成yx.json文件的解析(遥信模块信息)[修改过]
    5、完成yk.json文件的解析(遥控模块信息)
    6、完成com.json文件的解析(串口模块信息)[修改过]
    7、完成SPI.json文件的解析(SPI模块信息)
    8、完成SD.json文件的解析(SD模块信息)
    9、完成USB.json文件的解析(USB模块信息)
    10、完成DBG.json文件的解析
    11、完成HMI.json文件的解析(人机模块信息)
    12、完成eth.hson文件的解析(以太网模块信息)